\begin{table}[h!]
  \begin{center}
    \caption{Fachlicher Soll-Ist Vergleich}
    \label{table1}
    \pgfplotstabletypeset[
      multicolumn names, % allows to have multicolumn names
      col sep=comma, % the seperator in our .csv file
      header=has colnames,
      columns={Ziel,Erreicht},
      columns/Ziel/.style={column type={p{.6\textwidth}},string type},
      columns/Erreicht/.style={column type={p{.2\textwidth}},string type},
      every head row/.style={
        before row={\toprule}, % have a rule at top
        after row={\midrule}
      },
      every last row/.style={
        after row=\bottomrule % rule at bottom
      }
    ]{csv/fachlichersollist.csv} % filename/path to file
  \end{center}
\end{table}
